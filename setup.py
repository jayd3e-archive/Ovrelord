from setuptools import setup

requires = [
    'retools'
    ]

setup(name='overlord',
      version='0.0',
      description='Overlord',
      packages=['overlord'],
      install_requires=requires,
      )
